# Release Notes — v243

Date: 2025-11-21

Summary:
- Release v243 contains UI polish and CHANGELOG maintenance.
- Visible UI labels standardized to `Qtr`/`Qtrs`.
- Leaderboard rows now display bracketed quarter info on its own stacked line for consistent alignment.
- Deployment updated so the existing webapp URL serves v243.

Action items for owners:
- Visit the webapp URL and confirm the Insights dashboards render correctly (Offense hub — Singles/Pairs, Defensive views).
- Verify owner-only debug toggle visibility and that toasts/announce functions operate as expected.

Reference:
- Git tag: `v243`
- See `CHANGELOG.md` and `CONTRIBUTING.md` for details on entries and how to document future changes.
